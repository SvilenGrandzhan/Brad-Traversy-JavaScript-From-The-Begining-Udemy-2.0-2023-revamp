// let amount = '100'
// //change string to number

// //first way to convert it to use parseInt function
// amount = parseInt(amount)

// // in case it is decimal use parseFloat
// amount = 99.5
// amount = parseFloat(amount)

// //second way to do it is unary method
// amount = +amount

// //third way to do it is with constructor
// amount = Number(amount)

// let amount = 100
// // change number to string

// //first way to do it is using method toString
// amount = amount.toString()
// ////////////// Note /////////////// string to number function takes a parameter the string, number to string is applying method / function to the number

// // second way to do it using String(function) / constructor
// amount = String(amount)

// //convert number to boolean
// let amount = 1
// amount = Boolean(amount)
// let amount = 0
// amount = Boolean(amount)

amount = 'Saki'
amount = parseInt(amount)
// NaN - Not a Number

console.log(
  'Logged Output amount:',
  amount,
  '  Type of amount is: ',
  typeof amount
)
